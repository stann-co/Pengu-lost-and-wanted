export class BscotchUtilError extends Error {
    constructor(message, asserter) {
        super(message);
        this.name = 'BscotchUtilError';
        Error.captureStackTrace(this, asserter || this.constructor);
    }
}
export function assert(claim, message, stackStart) {
    if (!claim) {
        throw new BscotchUtilError(message, stackStart || assert);
    }
}
export function isBoolean(thing) {
    return typeof thing === 'boolean';
}
export function isDate(thing) {
    return thing instanceof Date;
}
export function isValidDate(thing) {
    return isDate(thing) && !isNaN(thing.getTime());
}
export function assertValidDate(date) {
    assert(isValidDate(date), `${date} is not a valid date`, assertValidDate);
}
export function isArray(thing) {
    return Array.isArray(thing);
}
export function isEmptyArray(thing) {
    return isArray(thing) && thing.length === 0;
}
export function isNonEmptyArray(thing) {
    return isArray(thing) && thing.length > 0;
}
export function isNullish(thing) {
    return thing === null || thing === undefined;
}
export function isDefined(thing) {
    return thing !== undefined;
}
export function isNotNullish(thing) {
    return !isNullish(thing);
}
export function isString(thing) {
    return typeof thing === 'string';
}
export function isNumber(thing) {
    return typeof thing === 'number';
}
// We can make a function that checks if a string is empty or not,
// where the return *type* is `true` if it is definitely empty, `false`
// if it is definitely not empty, and the more general `boolean` if
// we can't tell.
export function isEmptyString(thing) {
    return (thing === '');
}
export function isNonEmptyString(thing) {
    return (isString(thing) && !isEmptyString(thing));
}
//# sourceMappingURL=types.js.map
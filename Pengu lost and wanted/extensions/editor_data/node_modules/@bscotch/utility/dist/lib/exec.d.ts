/// <reference types="node" />
import { SpawnOptions } from 'child_process';
/**
 * Execute a command in a child process.
 */
export declare function exec(command: string, commandArgs: (string | undefined | null | false)[], options?: SpawnOptions & {
    /**
     * If `true`:
     * - stdout/err will not be written to the console of the calling process
     * - the command will not be printed to the console of the calling process
     */
    silent?: boolean;
}): Promise<{
    stdout: string;
    stderr: string;
    code: number;
}>;
//# sourceMappingURL=exec.d.ts.map
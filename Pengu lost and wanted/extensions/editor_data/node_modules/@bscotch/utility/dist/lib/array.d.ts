import type { ArrayWrapped, Defined, EmptyArray, NotFalsey, NotNullish } from '../types/utility.js';
import type { Narrow } from '../types/utility/narrow.js';
/**
 * Create a generator that yields each
 * subsequent number from some start point
 * to some end point (inclusive). If the
 * second number is smaller than the first,
 * will yield descending. Numbers are floored.
 */
export declare function nextNumberUntil(start: number, end: number): Generator<number, void, unknown>;
declare const _default: {
    nextNumberUntil: typeof nextNumberUntil;
};
export default _default;
export declare function literal<T>(v: Narrow<T>): T;
export declare function filterTruthy<T>(array: T[]): NotFalsey<T>[];
export declare function filterDefined<T>(array: T[]): Defined<T>[];
export declare function filterNotNullish<T>(array: T[]): NotNullish<T>[];
/**
 * If the provided value is not an array,
 * wrap it in an array. If the value is undefined
 * will return an empty array.
 */
export declare function arrayWrapped<T>(item: T): T extends undefined ? EmptyArray : ArrayWrapped<T>;
/**
 * Return `true` if the `comparison` function returns true
 * when applied to each adjacent pair of values. For example,
 * can be used to determine if an array of numbers is increasing
 * with `(current,last)=>current>last`.
 *
 * If the array has zero or one entry, `true`
 * is returned.
 */
export declare function arrayEveryPair<ArrayOfComparables extends any[]>(arrayOfComparables: ArrayOfComparables, comparison: (currentValue: ArrayOfComparables[number], lastValue: ArrayOfComparables[number]) => boolean): boolean;
export declare function arrayWithoutNullish<T>(array: T[]): NotNullish<T>[];
/**
 * Return true if each value is greater than the last
 */
export declare function arrayIsIncreasing<ArrayOfValues extends any[]>(increasingArray: ArrayOfValues): boolean;
/**
 * Returns `true` if every entry is the same.
 * Uses a strict-equal (`===`) comparison by
 * default.
 */
export declare function arrayIsDuplicates<ArrayOfValues extends any[]>(arr: ArrayOfValues, options?: {
    loose?: boolean;
}): boolean;
/**
 * Return true if each value is greater than the last
 */
export declare function arrayIsDecreasing<ArrayOfValues extends any[]>(decreasingArray: ArrayOfValues): boolean;
type FirstItemArray<Item> = Item[] | [Item, ...any[]] | Readonly<[Item, ...any[]]> | EmptyArray;
/**
 * If not an array, return self. Otherwise return 0th item.
 */
export declare function arrayUnwrapped<Item>(items: FirstItemArray<Item> | Item): Item extends EmptyArray ? undefined : Item;
type SortReturn = number;
/**
 * Ascending-sort an array of numeric values.
 * Can call on an array, or pass to `Array.sort`
 *
 * ```js
 * const array = [10,3,11];
 * // Both ways work
 * arraySortNumeric(array);
 * array.sort(arraySortNumeric);
 * ```
 */
export declare function arraySortNumeric<N extends number[]>(numbers: N): number[];
export declare function arraySortNumeric<N extends number>(array1Item: N, array2Item: N): SortReturn;
/**
 * Descending-sort an array of numeric values.
 * Can call on an array, or pass to `Array.sort`
 *
 * ```js
 * const array = [10,3,11];
 * // Both ways work
 * arraySortNumericDescending(array);
 * array.sort(arraySortNumericDescending);
 * ```
 */
export declare function arraySortNumericDescending<N extends number[]>(numbers: N): number[];
export declare function arraySortNumericDescending<N extends number>(array1Item: N, array2Item: N): SortReturn;
/**
 * Find an object in an array by one of its fields.
 *
 * For the value, use {@link findByField}
 */
export declare function findIndexByField<T>(arr: T[], field: keyof T, value: T[keyof T]): number;
/**
 * Find an object in an array by one of its fields.
 *
 * For the index position, use {@link findIndexByField}
 */
export declare function findByField<T>(arr: T[], field: keyof T, value: T[keyof T]): T;
/**
 * Find an object in an array, and its index position, by one of its fields.
 */
export declare function findEntryAndIndexByField<T>(arr: T[], field: keyof T, value: T[keyof T]): [entry: T, index: number];
//# sourceMappingURL=array.d.ts.map
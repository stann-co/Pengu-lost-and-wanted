comments should not reflect chat conversations but just explain how functions or complicated code works.
And comments should not be longer than 90 characters per line, and 2 lines max
But less text is always preferred, ideally the code is intuitive to follow by itself
Never reference past/prior code, old behavior, or this conversation in comments - describe
the current code as it is, not how or why it changed

Do not make extra complicated functions where reusing existing systems can suffice.

Try to not nest code needlessly unless referenced by user code
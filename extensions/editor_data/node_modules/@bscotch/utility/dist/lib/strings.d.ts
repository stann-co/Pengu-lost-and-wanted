/// <reference types="node" />
import type { ArrayOrSingleton, RegExpExecArrayWithIndices } from '../types/utility.js';
export declare function sortStringsByLength(strings: string[]): string[];
export declare function getShortestString(strings: string[]): string;
/**
 * Shift all lines left by the *smallest* indentation level,
 * and remove initial newline and all trailing spaces.
 * Lines that only have spaces are not used to determine the
 * indentation level.
 *
 * @example
 * undent`
 *  All lines will
 * be un-inindented
 *    based on the line
 *  with the smallest indentation.
 * `;
 * // =>
 * //`  All lines will
 * //be un-inindented
 * //    based on the line
 * //  with the smallest indentation.`
 *
 */
export declare function undent(strings: TemplateStringsArray, ...interps: any[]): string;
/**
 * Remove ALL indents, from every line.
 *
 * @example
 * nodent`
 *  All lines will
 * be un-indented
 *    completely
 *  but still on separate lines.
 * `;
 * // =>
 * //`All lines will
 * //be un-indented
 * //completely
 * //but still on separate lines.`
 *
 */
export declare function nodent(strings: TemplateStringsArray, ...interps: any[]): string;
/**
 * Remove linebreaks and extra spacing in a template string.
 *
 * @example
 * oneline`
 *  This string
 *      will be converted
 *  into
 *            one that is
 *  on a single line.
 * `;
 * // => "This string will be converted into one that is on a single line."
 */
export declare function oneline(strings: TemplateStringsArray, ...interps: any[]): string;
export declare function encodeToBase64(content: string | Buffer): string;
export declare function decodeFromBase64(base64: string): string;
export declare function decodeFromBase64JsonString(string: string): any;
export declare function encodeToBase64JsonString(something: any): string;
export declare function capitalize(string: string): string;
/**
 * Explode a string using a separator.
 */
export declare function explode(string: string | undefined, options?: {
    /** Only return first `limit` results (returns all by default) */
    limit?: number | null;
    /** Separator to explode on */
    sep?: string | RegExp;
    /** Nullstrings are skipped unless this is set to `true` */
    keepEmpty?: boolean;
    /** If `true`, only unique values returned (order not guaranteed) */
    unique?: boolean;
    /** By default the original string and all values are trimmed.
     * Set to `true` to prevent this behavior.
     */
    noTrim?: boolean;
}): string[];
/**
 * Check a string for a match against one or
 * more patterns. String patterns are tested
 * for an **exact** match, while RegExp patterns
 * are tested with `.test()`.
 *
 * Returns `true` if *any* patterns match.
 */
export declare function stringIsMatch(str: string, pattern: ArrayOrSingleton<RegExp | string>): boolean;
export declare function matchAll<Groups extends string = string>(string: string, pattern: RegExp): IterableIterator<RegExpExecArrayWithIndices<Groups>>;
//# sourceMappingURL=strings.d.ts.map